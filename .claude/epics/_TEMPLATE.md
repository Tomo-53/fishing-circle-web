# エピック: <案件名>

<!--
案件ノート（_epic.md）。1 エピック = 1 機能（基本は 1 `feature/` ブランチ）。
タスクを完了するたびに、決定事項・前提・学び・未解決事項をここへ「昇格」して永続化する。
個別タスクファイル（tasks/NNN-*.md）は完了後に削除してよいが、知識はここに残す。
機密（個人情報・釣果の座標・`.env` 値・認証情報）は絶対に書かない（.claude/rules/security.md）。
-->

- **slug**: `<slug>`
- **ブランチ**: `feature/<内容>`
- **作成日**: <YYYY-MM-DD>
- **状態**: 進行中 <!-- 進行中 / 保留 / 完了 -->

## 概要

この案件で何を達成するか（1〜3文）。背景と目的を簡潔に。

## 決定事項

<!-- 「なぜそうしたか」が分かる形で。後から読み返して意図が追えること。 -->
- (例) 釣果の公開範囲はグループ単位とし、`check.group.permission:2` で保護する。

## 前提

<!-- このエピックが依存している事実・制約・環境。 -->
- (例) `UserGroup` ピボットで user_id × group_id の権限を持つ。

## 学び

<!-- 実装中に分かった知見・ハマりどころ・再利用できるノウハウ。 -->
- (例) N+1 を避けるため一覧は `with('user')` で eager load する。

## 未解決事項

<!-- 次にやるべきこと・保留中の判断・TODO。/next の配膳元になる。 -->
- [ ] (例) 画像アップロードのサイズ上限を決める。

## タスク履歴

<!-- 完了タスクの記録（tasks/ のファイルを消しても要点が残るように）。 -->
- <YYYY-MM-DD> 001 ○○を実装（決定/学びは上のセクションへ昇格済み）
